approval_policy = "on-request"
sandbox_mode = "workspace-write"
model_reasoning_effort = "high"
[windows]
sandbox = "unelevated"
[tools]
web_search = true


[agents.default]
description = "Default agent for Python development in this project"
instructions = """
Always use the local .venv environment if it exists.
Never use system Python.
"""

[sandbox_workspace_write]
network_access = true
