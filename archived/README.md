# Archived Materials

This directory stores material that is intentionally outside the active product scope.

## Classification

| Path | Status | Notes |
| --- | --- | --- |
| `ENTRY_EXIT_AND_RISK_SPEC.md` | obsolete_reference | Final trade execution and exit management are out of active scope. Keep only as historical context. |
| `ENTRY_STRUCTURE_NOTES.md` | obsolete_reference | Legacy entry/risk notes. Active entry evaluation now lives in `src/signals/` and SystemDocs. |
| `src/` | unknown_legacy | Legacy source tree. Do not use for active behavior without explicit review. |
| `tests/` | unknown_legacy | Legacy tests. Do not use for active verification without explicit review. |

## Rules

- Do not treat these files as active specifications.
- Do not restore code from here into active modules without a focused review.
- Prefer new active documentation under `doc/SystemDocs/` or user-facing notes under `doc/ForCodexOutput/`.
