app:
  default_symbols:
  - NVDA
  - META
  - PLTR
  - CRWD
  - ANET
  - DUOL
  - APP
  - SNOW
  benchmark_symbol: SPY
  vix_symbol: ^VIX
  price_period: 3y
  cache_dir: C:/reository/shared_market_cache
  snapshot_dir: data_runs
  user_preferences_path: data_cache/user_preferences.yaml
  use_sample_data_if_fetch_fails: false
  refresh_on_start: true
