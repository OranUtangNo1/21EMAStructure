interface:
  display_name: "Session Handoff Resume"
  short_description: "Resume work from a saved handoff"
  default_prompt: "Use $handoff-resume to read the latest handoff and reconstruct the current work state before continuing."
